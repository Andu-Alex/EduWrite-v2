# Chat页面调试指南

## 问题分析

### 1. 500错误原因
- 原代码中调用了API接口，但后端可能没有正确配置
- 解决方案：完全避免API调用，使用模拟数据

### 2. 进度条不变化原因
- 路线图步骤更新和文本生成没有同步
- 解决方案：实现真正的同步机制

### 3. 文本生成失败原因
- 分离的路线图推进和文本生成导致不同步
- 解决方案：合并为一个同步函数

## 修复内容

### 1. 避免API调用错误
```javascript
// 修改前：会调用API导致500错误
homeStore.setMyData({
  act: "gpt.submit",
  actData: { prompt: prompt.value, uuid },
})

// 修改后：跳过API调用
console.log('普通聊天模式，跳过API调用')
```

### 2. 实现同步生成
```javascript
// 修改前：分离的路线图推进和文本生成
autoProgressRoadmap()
simulateAITextGeneration()

// 修改后：同步进行
simulateAITextGenerationWithRoadmap()
```

### 3. 添加调试信息
- 在关键函数中添加console.log
- 监控步骤推进和文本生成过程
- 确保路线图状态正确更新

## 测试步骤

### 1. 启动项目
```bash
npm run dev
```

### 2. 访问chat页面
- 打开浏览器开发者工具
- 访问 `http://localhost:5173/chat`
- 查看控制台输出

### 3. 观察调试信息
应该看到以下日志：
```
开始同步生成，步骤数量: 5 论文类型: literature
开始第一个步骤: 0 {id: 'problem', title: '问题提出', ...}
添加第一个回复: 很好！您提出了一个非常有价值的文献研究问题...
定时器执行，当前步骤: 1 总步骤: 5 当前回复: 1 总回复: 5
更新步骤到: 1 {id: 'concept', title: '概念梳理', ...}
添加回复: 基于文献研究的特点，我建议采用以下研究框架...
```

### 4. 验证功能
- 页面加载后立即显示欢迎消息
- 输入文本后立即开始生成
- 右侧路线图步骤依次亮起
- 每3秒推进一个步骤并生成一个回复

## 预期效果

1. **页面加载**：立即显示欢迎消息和路线图
2. **用户输入**：立即开始同步生成过程
3. **路线图**：步骤依次亮起（绿色边框）
4. **文本生成**：打字机效果逐字显示
5. **进度指示**：显示当前步骤（如：2/5）
6. **完成跳转**：所有步骤完成后跳转到agent页面

## 故障排除

如果功能仍然不工作：

1. **检查控制台**：查看是否有错误信息
2. **检查路线图**：确认showRoadmap为true
3. **检查步骤**：确认currentSteps有数据
4. **检查定时器**：确认定时器正常执行
5. **检查状态**：确认isGenerating状态正确

## 关键修复点

1. **避免API调用**：完全使用模拟数据
2. **同步机制**：路线图步骤和文本生成完全同步
3. **调试信息**：添加详细的控制台输出
4. **状态管理**：确保所有状态正确更新
5. **错误处理**：避免任何可能导致错误的操作
